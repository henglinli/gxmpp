aux_source_directory(. chat_SRC)
add_executable(gxmpp_chat ${chat_SRC})
if(UNIX)
	if(APPLE)
   		target_link_libraries(gxmpp_chat gxmpp "-framework Cocoa")
	else()
 		target_link_libraries(gxmpp_chat gxmpp pthread rt)
	endif()
endif()