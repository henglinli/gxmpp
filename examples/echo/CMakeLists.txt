if(UNIX)
  if(APPLE)
    aux_source_directory(. echo_SRC)
  else()
    file(GLOB echo_SRC "*.cc" "*.cpp")
  endif()
endif()
add_definitions(-std=c++11)
add_executable(gxmpp_echo ${echo_SRC})
target_link_libraries(gxmpp_echo gxmpp pthread rt)
